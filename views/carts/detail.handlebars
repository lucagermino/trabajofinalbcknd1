<h1>Carrito</h1>

<ul>
    {{#each cart.products}}
    <li>
        <h3>{{this.product.title}}</h3>
        <p>Precio: ${{this.product.price}}</p>
        <p>Cantidad: {{this.quantity}}</p>
    </li>
    {{/each}}
</ul>